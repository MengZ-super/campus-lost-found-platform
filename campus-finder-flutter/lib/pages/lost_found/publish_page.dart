import 'package:flutter/material.dart';

class PublishLostFoundPage extends StatelessWidget {
  const PublishLostFoundPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('发布信息')),
      body: const Center(
        child: Text('发布页（建设中）'),
      ),
    );
  }
}
